[request_definition]
r = sub, dom, obj, act

[policy_definition]
p = role, dom, obj, act

[role_definition]
g = _, _, _
g2 = _, _

[policy_effect]
e = some(where (p.eft == allow))

[matchers]
m = (g(r.sub, p.role, r.dom) || g2(r.sub, p.role)) && r.dom == p.dom && r.obj == p.obj && r.act == p.act