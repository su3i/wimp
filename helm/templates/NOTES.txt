WIMP has been installed.

Control plane ({{ include "wimp.cpFullname" . }}):
{{- if .Values.ingress.enabled }}
  URL: http{{ if .Values.ingress.tls.enabled }}s{{ end }}://{{ .Values.ingress.host }}
{{- else if eq .Values.service.type "LoadBalancer" }}
  Run: kubectl get svc {{ include "wimp.cpFullname" . }} -n {{ .Release.Namespace }}
  Wait for EXTERNAL-IP, then access at http://<EXTERNAL-IP>:{{ .Values.service.port }}
{{- else if eq .Values.service.type "NodePort" }}
  http://<NODE-IP>:{{ .Values.service.nodePort }}
{{- else }}
  Port-forward: kubectl port-forward svc/{{ include "wimp.cpFullname" . }} 8080:{{ .Values.service.port }} -n {{ .Release.Namespace }}
  Then open:    http://localhost:8080
{{- end }}

Web UI ({{ include "wimp.webFullname" . }}):
{{- if .Values.web.ingress.enabled }}
  URL: http{{ if .Values.web.ingress.tls.enabled }}s{{ end }}://{{ .Values.web.ingress.host }}
{{- else if eq .Values.web.service.type "LoadBalancer" }}
  Run: kubectl get svc {{ include "wimp.webFullname" . }} -n {{ .Release.Namespace }}
  Wait for EXTERNAL-IP, then access at http://<EXTERNAL-IP>:{{ .Values.web.service.port }}
{{- else if eq .Values.web.service.type "NodePort" }}
  http://<NODE-IP>:{{ .Values.web.service.nodePort }}
{{- else }}
  Port-forward: kubectl port-forward svc/{{ include "wimp.webFullname" . }} 8080:80 -n {{ .Release.Namespace }}
  Then open:    http://localhost:8080
{{- end }}

Default admin login:
  Username:    {{ .Values.config.defaultAdminUsername }}
  Password: {{ .Values.config.defaultAdminPassword }}
